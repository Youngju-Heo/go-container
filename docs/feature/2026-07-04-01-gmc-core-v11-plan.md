# GMC 코어 v1.1 확장 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**목표:** 설계 문서(`docs/feature/2026-07-04-01-gmc-core-v11-design.md`)의 코어 확장 — DTS 확장점(flagHasDTS)·Reordered 쓰기 모드·maxPTS 요약·LastPTS/LastTime·SeekTime·Finalized 를 구현한다.

**아키텍처:** 유일한 온디스크 변경은 Data payload의 추가적 flags 비트(0x02=hasDTS, 설정 시 pts 뒤 dts u64). 나머지는 writer 검증 규칙·trackState 확장·Reader accessor 추가다. 기존 v1 파일과 100% 호환, dts 미사용 파일은 바이트 동일.

**기술 스택:** Go 1.22+, 표준 라이브러리만 (`math/bits` 추가 사용).

## 전역 제약

- 모듈 `github.com/Youngju-Heo/go-container`, 패키지 `gmc/` (white-box 테스트, `package gmc`)
- 바이트 오더 리틀 엔디언, 헤더 version 1 유지 (추가적 변경만)
- 코드 내 로그·에러·주석은 영어, 커밋 메시지는 한국어
- 모든 태스크 TDD (실패 테스트 → 구현 → 통과 → 커밋), 테스트는 리포 루트에서 `go test ./gmc/`
- 이 플랫폼(windows/arm64)은 `-race` 미지원 — 전체 회귀는 일반 모드로 실행하고, `-race`는 지원 플랫폼/CI에서 재실행한다고 계획 말미에 기록

## 파일 구조

```
gmc/
  data.go       — flagHasDTS, dataHeader 구조체, dts 인코딩/디코딩 (태스크 1)
  writer.go     — Frame.DTS/HasDTS emit(태스크 1), 검증 규칙·trackState 확장 (태스크 2)
  iterator.go   — dataHeader 이행 (태스크 1)
  reader.go     — scan/loadFooter maxPTS (태스크 3), LastPTS/LastTime (태스크 4),
                  SeekTime (태스크 5), Finalized (태스크 6)
  timeconv.go   — mulDiv64·ptsToNano·nanoToPTS (태스크 4)
  format.go     — ErrNoStartTime (태스크 5)
  *_test.go     — 각 태스크의 테스트
```

---

### 태스크 1: Data payload DTS 코덱 + Frame API + 호출처 이행

**Files:**
- Modify: `gmc/data.go` (전체 교체 수준), `gmc/writer.go:14-18` (Frame), `gmc/writer.go:177-183` (emit), `gmc/iterator.go:38-52` (Next), `gmc/iterator.go:115-126` (follow), `gmc/reader.go:100-102` (scan)
- Modify(테스트 이행): `gmc/track_test.go:37-53`, `gmc/writer_test.go:99-104`
- Test: `gmc/data_test.go` (신규)

**Interfaces:**
- Consumes: `ErrCorrupt`, `TrackID`, `flagKeyframe` (기존)
- Produces: `flagHasDTS byte = 0x02`, `dataHeaderDTSSize = 19`, `type dataHeader struct { id TrackID; flags byte; pts, dts uint64; n int }`, `encodeDataPayload(dst []byte, id TrackID, flags byte, pts, dts uint64, data []byte) []byte`, `decodeDataHeader(p []byte) (dataHeader, error)` — 프레임 본문은 `p[h.n:]`
- Produces(공개): `Frame{ PTS, DTS uint64; HasDTS, Keyframe bool; Data []byte }` — WriteFrame은 HasDTS면 flagHasDTS를 세우고 dts를 기록, Iterator/Follow는 복원

- [ ] **Step 1: 실패하는 테스트 작성** — `gmc/data_test.go` (신규)

```go
package gmc

import (
	"bytes"
	"errors"
	"testing"
)

func TestDataPayloadWithDTS(t *testing.T) {
	body := []byte("bframe")
	p := encodeDataPayload(nil, 7, flagKeyframe|flagHasDTS, 6000, 3000, body)
	if len(p) != dataHeaderDTSSize+len(body) {
		t.Fatalf("len = %d, want %d", len(p), dataHeaderDTSSize+len(body))
	}
	h, err := decodeDataHeader(p)
	if err != nil {
		t.Fatal(err)
	}
	if h.id != 7 || h.flags != flagKeyframe|flagHasDTS || h.pts != 6000 || h.dts != 3000 || h.n != dataHeaderDTSSize {
		t.Fatalf("h = %+v", h)
	}
	if !bytes.Equal(p[h.n:], body) {
		t.Fatal("body mismatch")
	}
	// truncated dts -> corrupt
	if _, err := decodeDataHeader(p[:15]); !errors.Is(err, ErrCorrupt) {
		t.Fatalf("truncated dts: err = %v", err)
	}
}

func TestDataPayloadWithoutDTSUnchanged(t *testing.T) {
	// dts argument must be ignored when flagHasDTS is not set (v1 byte layout)
	p := encodeDataPayload(nil, 5, flagKeyframe, 90000, 12345, []byte("x"))
	if len(p) != dataHeaderSize+1 {
		t.Fatalf("len = %d, want %d", len(p), dataHeaderSize+1)
	}
	h, err := decodeDataHeader(p)
	if err != nil {
		t.Fatal(err)
	}
	if h.n != dataHeaderSize || h.dts != 0 || h.pts != 90000 {
		t.Fatalf("h = %+v", h)
	}
}

func TestWriteFrameEmitsDTS(t *testing.T) {
	w, path := newTestWriter(t, CreateOptions{CheckpointBytes: 1 << 30, CheckpointInterval: time.Hour})
	video, err := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000})
	if err != nil {
		t.Fatal(err)
	}
	if err := w.WriteFrame(video, Frame{PTS: 3000, DTS: 0, HasDTS: true, Keyframe: true, Data: []byte("kf")}); err != nil {
		t.Fatal(err)
	}
	chunks := scanChunks(t, path, w.streamStart, w.committed.Load())
	h, err := decodeDataHeader(chunks[1].payload)
	if err != nil || h.flags&flagHasDTS == 0 || h.pts != 3000 || h.dts != 0 {
		t.Fatalf("h=%+v err=%v", h, err)
	}
	// reader roundtrip restores DTS/HasDTS
	r, err := w.NewReader()
	if err != nil {
		t.Fatal(err)
	}
	defer r.Close()
	it, err := r.SeekPTS(video, 3000)
	if err != nil {
		t.Fatal(err)
	}
	if !it.Next() {
		t.Fatal(it.Err())
	}
	fr := it.Frame()
	if !fr.HasDTS || fr.DTS != 0 || fr.PTS != 3000 || !bytes.Equal(fr.Data, []byte("kf")) {
		t.Fatalf("frame = %+v", fr)
	}
	w.Close()
}
```

`gmc/data_test.go` 상단 import에 `"time"` 추가 필요 (`TestWriteFrameEmitsDTS`가 사용).

- [ ] **Step 2: 실패 확인**

Run: `go test ./gmc/ -run "TestDataPayload|TestWriteFrameEmitsDTS" -v`
Expected: `FAIL ... [build failed]` (undefined: flagHasDTS, dataHeader 등)

- [ ] **Step 3: 구현 — data.go 교체**

`gmc/data.go` 전체를 다음으로 교체:

```go
package gmc

import "encoding/binary"

const (
	// dataHeaderSize is trackID(2) + flags(1) + pts(8) without DTS.
	dataHeaderSize = 11
	// dataHeaderDTSSize additionally includes the optional dts(8).
	dataHeaderDTSSize = 19

	flagKeyframe byte = 0x01
	flagHasDTS   byte = 0x02
)

// dataHeader is the decoded fixed header of a Data payload. n is the header
// length in bytes (11, or 19 when flagHasDTS is set); the frame body is p[n:].
type dataHeader struct {
	id    TrackID
	flags byte
	pts   uint64
	dts   uint64 // valid only when flags&flagHasDTS != 0
	n     int
}

// encodeDataPayload serializes a Data payload. dts is written only when
// flags&flagHasDTS is set.
func encodeDataPayload(dst []byte, id TrackID, flags byte, pts, dts uint64, data []byte) []byte {
	dst = binary.LittleEndian.AppendUint16(dst, uint16(id))
	dst = append(dst, flags)
	dst = binary.LittleEndian.AppendUint64(dst, pts)
	if flags&flagHasDTS != 0 {
		dst = binary.LittleEndian.AppendUint64(dst, dts)
	}
	return append(dst, data...)
}

func decodeDataHeader(p []byte) (dataHeader, error) {
	if len(p) < dataHeaderSize {
		return dataHeader{}, ErrCorrupt
	}
	h := dataHeader{
		id:    TrackID(binary.LittleEndian.Uint16(p)),
		flags: p[2],
		pts:   binary.LittleEndian.Uint64(p[3:11]),
		n:     dataHeaderSize,
	}
	if h.flags&flagHasDTS != 0 {
		if len(p) < dataHeaderDTSSize {
			return dataHeader{}, ErrCorrupt
		}
		h.dts = binary.LittleEndian.Uint64(p[11:19])
		h.n = dataHeaderDTSSize
	}
	return h, nil
}
```

- [ ] **Step 4: 구현 — Frame 확장과 emit** (`gmc/writer.go`)

Frame 정의(13~18행)를 교체:

```go
// Frame is one media frame / sample / metadata event. Frames are stored in
// decode order; PTS is the presentation timestamp. DTS (decode timestamp) is
// optional and stored only when HasDTS is set.
type Frame struct {
	PTS      uint64
	DTS      uint64 // valid only when HasDTS
	HasDTS   bool
	Keyframe bool
	Data     []byte
}
```

`WriteFrame`의 flags/encode 부분(177~183행)을 교체 — 검증 규칙 변경은 태스크 2에서, 여기서는 emit만:

```go
	var flags byte
	if fr.Keyframe {
		flags |= flagKeyframe
	}
	if fr.HasDTS {
		flags |= flagHasDTS
	}
	w.scratch = encodeDataPayload(w.scratch[:0], id, flags, fr.PTS, fr.DTS, fr.Data)
```

- [ ] **Step 5: 구현 — 호출처 이행**

`gmc/iterator.go` `Next()`의 디코드 블록(38~52행)을 교체:

```go
		h, derr := decodeDataHeader(payload)
		if derr != nil {
			it.err = derr
			return false
		}
		if it.filter != nil && !it.filter[h.id] {
			continue
		}
		it.track = h.id
		it.frame = Frame{
			PTS:      h.pts,
			DTS:      h.dts,
			HasDTS:   h.flags&flagHasDTS != 0,
			Keyframe: h.flags&flagKeyframe != 0,
			Data:     append([]byte(nil), payload[h.n:]...),
		}
		return true
```

`gmc/iterator.go` `follow()`의 디코드 블록(115~126행)을 교체:

```go
			h, derr := decodeDataHeader(payload)
			if derr != nil {
				return
			}
			if filter != nil && !filter[h.id] {
				continue
			}
			tf := TrackFrame{Track: h.id, Frame: Frame{
				PTS:      h.pts,
				DTS:      h.dts,
				HasDTS:   h.flags&flagHasDTS != 0,
				Keyframe: h.flags&flagKeyframe != 0,
				Data:     append([]byte(nil), payload[h.n:]...),
			}}
```

`gmc/reader.go` `scan()`의 chunkData 케이스(99~102행)를 교체:

```go
		case chunkData:
			if h, derr := decodeDataHeader(payload); derr == nil && h.flags&flagKeyframe != 0 {
				tail = append(tail, cpEntry{h.id, h.pts, off})
			}
```

- [ ] **Step 6: 기존 테스트 이행**

`gmc/track_test.go`의 `TestDataPayloadRoundtrip`(37~53행)을 새 시그니처로 교체:

```go
func TestDataPayloadRoundtrip(t *testing.T) {
	body := []byte("frame-bytes")
	p := encodeDataPayload(nil, 5, flagKeyframe, 90000, 0, body)
	if len(p) != dataHeaderSize+len(body) {
		t.Fatalf("len = %d", len(p))
	}
	h, err := decodeDataHeader(p)
	if err != nil {
		t.Fatal(err)
	}
	if h.id != 5 || h.flags != flagKeyframe || h.pts != 90000 || !bytes.Equal(p[h.n:], body) {
		t.Fatalf("h=%+v", h)
	}
	if _, err := decodeDataHeader(p[:10]); !errors.Is(err, ErrCorrupt) {
		t.Fatalf("short header: err = %v", err)
	}
}
```

`gmc/writer_test.go`의 디코드 검증부(99~104행)를 교체:

```go
	h, err := decodeDataHeader(chunks[1].payload)
	if err != nil || h.id != video || h.flags != flagKeyframe || h.pts != 0 {
		t.Fatalf("frame0: h=%+v err=%v", h, err)
	}
	if !bytes.Equal(chunks[2].payload[dataHeaderSize:], []byte("p1")) {
		t.Fatal("frame1 body mismatch")
	}
```

- [ ] **Step 7: 통과 확인 (전체 회귀 포함)**

Run: `go test ./gmc/`
Expected: `ok` (신규 3개 + 기존 전체 PASS)

- [ ] **Step 8: 커밋**

```powershell
git add gmc/
git commit -m "구현: Data payload DTS 확장점 (flagHasDTS 비트, 선택적 dts 필드, Frame.DTS/HasDTS)"
```

---

### 태스크 2: WriteFrame 검증 규칙 — 유효 DTS 단조 + Reordered 모드 + maxPTS

**Files:**
- Modify: `gmc/writer.go` — `trackState`(28~35행), `WriteFrame` 검증·갱신부(170~196행), `Finalize` sums(330~334행), `gmc/track.go` — `TrackInfo`에 Reordered 필드
- Test: `gmc/writer_reorder_test.go` (신규)

**Interfaces:**
- Consumes: 태스크 1의 `Frame.HasDTS/DTS`, `flagHasDTS`
- Produces(공개): `TrackInfo.Reordered bool` — 직렬화되지 않는 쓰기 검증 모드
- Produces(내부): `trackState{ info TrackInfo; hasLast, hasKey bool; firstPTS, lastEff, lastKeyPTS, maxPTS, frames uint64; indexedThisCP bool }` — `lastPTS` 필드 제거, 태스크 3·4가 `maxPTS` 사용
- 검증 규칙: 기본 모드 = 유효 디코드 ts(=DTS, 없으면 PTS) 단조 비감소 / Reordered = Keyframe pts만 직전 키프레임 pts 이상

- [ ] **Step 1: 실패하는 테스트 작성** — `gmc/writer_reorder_test.go`

```go
package gmc

import (
	"errors"
	"testing"
	"time"
)

// IBBP-style GOP: decode order with non-monotonic pts (B-frames).
// pts step 3000, GOP: I(0) P(9000) B(3000) B(6000) | I(12000) ...
func writeReorderedGOP(t *testing.T, w *Writer, id TrackID, gops int) {
	t.Helper()
	for g := 0; g < gops; g++ {
		base := uint64(g * 12000)
		frames := []struct {
			pts uint64
			key bool
		}{
			{base, true}, {base + 9000, false}, {base + 3000, false}, {base + 6000, false},
		}
		for i, fr := range frames {
			if err := w.WriteFrame(id, Frame{PTS: fr.pts, Keyframe: fr.key, Data: []byte{byte(g), byte(i)}}); err != nil {
				t.Fatalf("gop %d frame %d: %v", g, i, err)
			}
		}
	}
}

func TestReorderedTrackAcceptsBFrames(t *testing.T) {
	w, _ := newTestWriter(t, CreateOptions{CheckpointBytes: 1 << 30, CheckpointInterval: time.Hour})
	video, err := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000, Reordered: true})
	if err != nil {
		t.Fatal(err)
	}
	writeReorderedGOP(t, w, video, 3)

	// keyframe pts regression is still rejected
	if err := w.WriteFrame(video, Frame{PTS: 11000, Keyframe: true}); !errors.Is(err, ErrNonMonotonicPTS) {
		t.Fatalf("keyframe regression: err = %v", err)
	}
	// maxPTS tracks the highest pts, not the last written one
	if got := w.tracks[video].maxPTS; got != 2*12000+9000 {
		t.Fatalf("maxPTS = %d, want %d", got, 2*12000+9000)
	}

	// seek to pts 15000 starts at GOP-1 keyframe (pts 12000) and covers the GOP
	r, err := w.NewReader()
	if err != nil {
		t.Fatal(err)
	}
	defer r.Close()
	it, err := r.SeekPTS(video, 15000)
	if err != nil {
		t.Fatal(err)
	}
	var ptss []uint64
	for it.Next() {
		ptss = append(ptss, it.Frame().PTS)
	}
	if it.Err() != nil {
		t.Fatal(it.Err())
	}
	if len(ptss) != 8 || ptss[0] != 12000 || ptss[1] != 21000 {
		t.Fatalf("ptss = %v", ptss)
	}
	w.Close()
}

func TestDefaultTrackStillStrict(t *testing.T) {
	w, _ := newTestWriter(t, CreateOptions{})
	video, _ := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000})
	if err := w.WriteFrame(video, Frame{PTS: 100, Keyframe: true}); err != nil {
		t.Fatal(err)
	}
	// non-monotonic pts without dts is still rejected on a default track
	if err := w.WriteFrame(video, Frame{PTS: 99}); !errors.Is(err, ErrNonMonotonicPTS) {
		t.Fatalf("err = %v", err)
	}
	w.Close()
}

func TestDTSMonotonicityOnDefaultTrack(t *testing.T) {
	w, _ := newTestWriter(t, CreateOptions{})
	video, _ := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000})
	// with explicit DTS, monotonicity is checked on DTS: pts may reorder
	if err := w.WriteFrame(video, Frame{PTS: 3000, DTS: 0, HasDTS: true, Keyframe: true}); err != nil {
		t.Fatal(err)
	}
	if err := w.WriteFrame(video, Frame{PTS: 9000, DTS: 1000, HasDTS: true}); err != nil {
		t.Fatal(err)
	}
	if err := w.WriteFrame(video, Frame{PTS: 6000, DTS: 2000, HasDTS: true}); err != nil {
		t.Fatalf("pts reorder with monotonic dts must pass: %v", err)
	}
	// dts regression is rejected
	if err := w.WriteFrame(video, Frame{PTS: 12000, DTS: 1500, HasDTS: true}); !errors.Is(err, ErrNonMonotonicPTS) {
		t.Fatalf("dts regression: err = %v", err)
	}
	w.Close()
}

func TestFinalizeSummaryUsesMaxPTS(t *testing.T) {
	w, path := newTestWriter(t, CreateOptions{CheckpointBytes: 1 << 30, CheckpointInterval: time.Hour})
	video, _ := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000, Reordered: true})
	writeReorderedGOP(t, w, video, 1) // last written pts 6000, max 9000
	if err := w.Finalize(); err != nil {
		t.Fatal(err)
	}
	f, err := os.Open(path)
	if err != nil {
		t.Fatal(err)
	}
	defer f.Close()
	fi, _ := f.Stat()
	var tb [trailerSize]byte
	f.ReadAt(tb[:], fi.Size()-trailerSize)
	footerOff, _ := decodeTrailer(tb[:])
	_, payload, _, err := readChunkAt(f, footerOff, fi.Size()-trailerSize)
	if err != nil {
		t.Fatal(err)
	}
	_, sums, _, err := decodeFooter(payload)
	if err != nil || len(sums) != 1 {
		t.Fatalf("sums=%v err=%v", sums, err)
	}
	if sums[0].lastPTS != 9000 {
		t.Fatalf("summary lastPTS = %d, want maxPTS 9000", sums[0].lastPTS)
	}
}
```

파일 상단 import에 `"os"` 포함.

- [ ] **Step 2: 실패 확인**

Run: `go test ./gmc/ -run "TestReordered|TestDefaultTrackStillStrict|TestDTSMonotonicity|TestFinalizeSummaryUsesMaxPTS" -v`
Expected: `FAIL ... [build failed]` (TrackInfo에 Reordered 없음)

- [ ] **Step 3: 구현 — TrackInfo.Reordered** (`gmc/track.go`)

`TrackInfo` 구조체 마지막에 필드 추가 (직렬화 코드는 건드리지 않는다):

```go
	// Reordered selects the write-side validation mode for this track. It is
	// not serialized (like ID, it is meaningless on read). When true the track
	// carries a reordered (B-frame) stream in decode order: only sync-point
	// (keyframe) pts monotonicity is enforced and non-keyframe pts are free.
	// Callers that can supply DTS should prefer the default mode with HasDTS.
	Reordered bool
```

- [ ] **Step 4: 구현 — trackState·WriteFrame·Finalize** (`gmc/writer.go`)

`trackState`(28~35행)를 교체:

```go
type trackState struct {
	info          TrackInfo
	hasLast       bool
	hasKey        bool
	firstPTS      uint64
	lastEff       uint64 // last effective decode ts (dts, or pts when absent)
	lastKeyPTS    uint64 // last keyframe pts (Reordered-mode monotonicity)
	maxPTS        uint64 // highest committed pts (summaries, LastPTS)
	frames        uint64
	indexedThisCP bool
}
```

`WriteFrame`의 검증부(174~176행의 `if ts.hasLast && fr.PTS < ts.lastPTS ...`)를 교체:

```go
	eff := fr.PTS
	if fr.HasDTS {
		eff = fr.DTS
	}
	if ts.hasLast {
		if ts.info.Reordered {
			if fr.Keyframe && ts.hasKey && fr.PTS < ts.lastKeyPTS {
				return ErrNonMonotonicPTS
			}
		} else if eff < ts.lastEff {
			return ErrNonMonotonicPTS
		}
	}
```

갱신부(186~191행의 `if !ts.hasLast { ... } ... ts.lastPTS = fr.PTS ...`)를 교체:

```go
	if !ts.hasLast {
		ts.firstPTS = fr.PTS
		ts.maxPTS = fr.PTS
	} else if fr.PTS > ts.maxPTS {
		ts.maxPTS = fr.PTS
	}
	ts.hasLast = true
	ts.lastEff = eff
	if fr.Keyframe {
		ts.hasKey = true
		ts.lastKeyPTS = fr.PTS
	}
	ts.frames++
```

`Finalize`의 sums 생성(333행)에서 `lastPTS: ts.lastPTS` → `lastPTS: ts.maxPTS`:

```go
		sums = append(sums, trackSummary{track: id, firstPTS: ts.firstPTS, lastPTS: ts.maxPTS, frames: ts.frames})
```

- [ ] **Step 5: 통과 확인 (전체 회귀 포함)**

Run: `go test ./gmc/`
Expected: `ok` — 기존 `TestWriteFrameValidation`(기본 모드 pts 검사)도 그대로 통과

- [ ] **Step 6: 커밋**

```powershell
git add gmc/
git commit -m "구현: 유효 DTS 단조 검증 및 Reordered 쓰기 모드 (키프레임 pts만 강제, maxPTS 요약)"
```

---

### 태스크 3: Reader maxPTS — footer summary 보존 + 스캔 추적

**Files:**
- Modify: `gmc/reader.go` — `Reader` 구조체(14~25행), `Open`(59~67행), `scan`(99~102행), `loadFooter`(131~140행)
- Test: `gmc/reader_maxpts_test.go` (신규)

**Interfaces:**
- Consumes: 태스크 1 `dataHeader`, 태스크 2 `maxPTS` 의미론
- Produces(내부): `Reader.maxPTS map[TrackID]uint64` — 비라이브 리더의 트랙별 최대 pts (프레임 있는 트랙만 엔트리 존재). 태스크 4의 `LastPTS`가 소비

- [ ] **Step 1: 실패하는 테스트 작성** — `gmc/reader_maxpts_test.go`

```go
package gmc

import (
	"os"
	"testing"
	"time"
)

func TestReaderMaxPTSFooterAndScan(t *testing.T) {
	w, path := newTestWriter(t, CreateOptions{CheckpointBytes: 1 << 30, CheckpointInterval: time.Hour})
	video, _ := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000, Reordered: true})
	writeReorderedGOP(t, w, video, 1) // pts 순서 0,9000,3000,6000 — max 9000
	if err := w.Finalize(); err != nil {
		t.Fatal(err)
	}

	// footer path
	r, err := Open(path)
	if err != nil {
		t.Fatal(err)
	}
	if got, ok := r.maxPTS[video]; !ok || got != 9000 {
		t.Fatalf("footer maxPTS = %d ok=%v, want 9000", got, ok)
	}
	r.Close()

	// scan path: truncate the trailer to force recovery
	fi, err := os.Stat(path)
	if err != nil {
		t.Fatal(err)
	}
	if err := os.Truncate(path, fi.Size()-trailerSize); err != nil {
		t.Fatal(err)
	}
	r2, err := Open(path)
	if err != nil {
		t.Fatal(err)
	}
	defer r2.Close()
	if got, ok := r2.maxPTS[video]; !ok || got != 9000 {
		t.Fatalf("scan maxPTS = %d ok=%v, want 9000", got, ok)
	}
	// empty-track semantics: unknown track has no entry
	if _, ok := r2.maxPTS[TrackID(99)]; ok {
		t.Fatal("unexpected entry for unknown track")
	}
}
```

- [ ] **Step 2: 실패 확인**

Run: `go test ./gmc/ -run TestReaderMaxPTSFooterAndScan -v`
Expected: `FAIL ... [build failed]` (r.maxPTS 없음)

- [ ] **Step 3: 구현** (`gmc/reader.go`)

`Reader` 구조체에 필드 추가 (`tracks` 아래):

```go
	maxPTS map[TrackID]uint64 // per-track max committed pts (non-live readers)
```

`Open`의 Reader 초기화(59~67행)에 추가:

```go
		maxPTS:      map[TrackID]uint64{},
```

`scan`의 chunkData 케이스(태스크 1에서 이행한 코드)를 교체:

```go
		case chunkData:
			if h, derr := decodeDataHeader(payload); derr == nil {
				if v, ok := r.maxPTS[h.id]; !ok || h.pts > v {
					r.maxPTS[h.id] = h.pts
				}
				if h.flags&flagKeyframe != 0 {
					tail = append(tail, cpEntry{h.id, h.pts, off})
				}
			}
```

`loadFooter`의 decodeFooter 호출부(131행)에서 summary를 보존:

```go
	tracks, sums, entries, err := decodeFooter(payload)
	if err != nil {
		return err
	}
	for _, tr := range tracks {
		r.tracks[tr.ID] = tr
	}
	for _, s := range sums {
		if s.frames > 0 {
			r.maxPTS[s.track] = s.lastPTS // stores maxPTS since task 2
		}
	}
```

- [ ] **Step 4: 통과 확인**

Run: `go test ./gmc/`
Expected: `ok`

- [ ] **Step 5: 커밋**

```powershell
git add gmc/reader.go gmc/reader_maxpts_test.go
git commit -m "구현: Reader 트랙별 maxPTS 적재 (footer summary 보존, 스캔 복구 추적)"
```

---

### 태스크 4: 128비트 시간 변환 + LastPTS / LastTime

**Files:**
- Create: `gmc/timeconv.go`
- Modify: `gmc/reader.go` — `trackInfo` 헬퍼, `LastPTS`, `LastTime` 추가
- Test: `gmc/timeconv_test.go`, `gmc/lastpts_test.go` (신규)

**Interfaces:**
- Consumes: 태스크 2 `trackState.maxPTS`·`hasLast`, 태스크 3 `Reader.maxPTS`, 기존 `StartTime()`
- Produces(내부): `mulDiv64(a, b, c uint64) uint64` — 128비트 중간곱, 몫 오버플로 시 MaxUint64 클램프, `ptsToNano(pts uint64, info TrackInfo) uint64`, `nanoToPTS(ns uint64, info TrackInfo) uint64`, `(r *Reader) trackInfo(id TrackID) (TrackInfo, bool)`
- Produces(공개): `(r *Reader) LastPTS(id TrackID) (uint64, bool)`, `(r *Reader) LastTime(id TrackID) (time.Time, bool)`
- 태스크 5의 SeekTime이 `nanoToPTS`·`trackInfo`를 소비

- [ ] **Step 1: 실패하는 테스트 작성** — `gmc/timeconv_test.go`

```go
package gmc

import (
	"math"
	"testing"
)

func TestMulDiv64(t *testing.T) {
	cases := []struct{ a, b, c, want uint64 }{
		{90000, 1_000_000_000, 90000, 1_000_000_000}, // exact
		{1, 1, 3, 0},                                  // truncation
		{math.MaxUint64, 2, 4, math.MaxUint64 / 2},    // 128-bit intermediate
		{math.MaxUint64, math.MaxUint64, 1, math.MaxUint64}, // quotient overflow -> clamp
	}
	for _, c := range cases {
		if got := mulDiv64(c.a, c.b, c.c); got != c.want {
			t.Fatalf("mulDiv64(%d,%d,%d) = %d, want %d", c.a, c.b, c.c, got, c.want)
		}
	}
}

func TestPTSNanoConversion(t *testing.T) {
	tb := TrackInfo{TimebaseNum: 1, TimebaseDen: 90000}
	if got := ptsToNano(90000, tb); got != 1_000_000_000 {
		t.Fatalf("ptsToNano = %d", got)
	}
	if got := nanoToPTS(1_000_000_000, tb); got != 90000 {
		t.Fatalf("nanoToPTS = %d", got)
	}
	// multi-day recording at 90 kHz: Δns×den overflows u64, must survive
	const days3 = uint64(3 * 24 * 3600 * 1_000_000_000)
	want := uint64(3*24*3600) * 90000
	if got := nanoToPTS(days3, tb); got != want {
		t.Fatalf("3-day nanoToPTS = %d, want %d", got, want)
	}
	// 1 ms timebase (MKV default)
	ms := TrackInfo{TimebaseNum: 1, TimebaseDen: 1000}
	if got := nanoToPTS(1_500_000_000, ms); got != 1500 {
		t.Fatalf("ms nanoToPTS = %d", got)
	}
}
```

- [ ] **Step 2: `gmc/lastpts_test.go` 작성**

```go
package gmc

import (
	"os"
	"testing"
	"time"
)

func TestLastPTSAndLastTime(t *testing.T) {
	w, path := newTestWriter(t, CreateOptions{CheckpointBytes: 1 << 30, CheckpointInterval: time.Hour})
	start := time.Unix(1_700_000_000, 0).UTC()
	if err := w.SetStartTime(start); err != nil {
		t.Fatal(err)
	}
	video, _ := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000, Reordered: true})
	empty, _ := w.AddTrack(TrackInfo{Kind: KindData, Codec: "json", TimebaseNum: 1, TimebaseDen: 1000})
	writeReorderedGOP(t, w, video, 1) // max pts 9000 = 0.1s

	// live reader
	r, err := w.NewReader()
	if err != nil {
		t.Fatal(err)
	}
	if pts, ok := r.LastPTS(video); !ok || pts != 9000 {
		t.Fatalf("live LastPTS = %d ok=%v", pts, ok)
	}
	if _, ok := r.LastPTS(empty); ok {
		t.Fatal("empty track must report ok=false")
	}
	if _, ok := r.LastPTS(TrackID(99)); ok {
		t.Fatal("unknown track must report ok=false")
	}
	wantTime := start.Add(100 * time.Millisecond)
	if got, ok := r.LastTime(video); !ok || !got.Equal(wantTime) {
		t.Fatalf("live LastTime = %v ok=%v, want %v", got, ok, wantTime)
	}
	r.Close()

	// finalized file
	if err := w.Finalize(); err != nil {
		t.Fatal(err)
	}
	r2, err := Open(path)
	if err != nil {
		t.Fatal(err)
	}
	if pts, ok := r2.LastPTS(video); !ok || pts != 9000 {
		t.Fatalf("footer LastPTS = %d ok=%v", pts, ok)
	}
	if got, ok := r2.LastTime(video); !ok || !got.Equal(wantTime) {
		t.Fatalf("footer LastTime = %v ok=%v", got, ok)
	}
	r2.Close()

	// crashed file (scan path)
	fi, _ := os.Stat(path)
	if err := os.Truncate(path, fi.Size()-trailerSize); err != nil {
		t.Fatal(err)
	}
	r3, err := Open(path)
	if err != nil {
		t.Fatal(err)
	}
	defer r3.Close()
	if pts, ok := r3.LastPTS(video); !ok || pts != 9000 {
		t.Fatalf("scan LastPTS = %d ok=%v", pts, ok)
	}
}

func TestLastTimeWithoutStartTime(t *testing.T) {
	w, _ := newTestWriter(t, CreateOptions{})
	video, _ := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000})
	if err := w.WriteFrame(video, Frame{PTS: 0, Keyframe: true}); err != nil {
		t.Fatal(err)
	}
	r, err := w.NewReader()
	if err != nil {
		t.Fatal(err)
	}
	defer r.Close()
	if _, ok := r.LastTime(video); ok {
		t.Fatal("LastTime without start time must report ok=false")
	}
	w.Close()
}
```

- [ ] **Step 3: 실패 확인**

Run: `go test ./gmc/ -run "TestMulDiv64|TestPTSNano|TestLastPTS|TestLastTime" -v`
Expected: `FAIL ... [build failed]`

- [ ] **Step 4: 구현 — `gmc/timeconv.go`**

```go
package gmc

import "math/bits"

// mulDiv64 returns a*b/c using a 128-bit intermediate product. The quotient
// is clamped to MaxUint64 when it does not fit (callers treat that as
// "beyond end of stream"). c must be non-zero.
func mulDiv64(a, b, c uint64) uint64 {
	hi, lo := bits.Mul64(a, b)
	if hi >= c {
		return ^uint64(0)
	}
	q, _ := bits.Div64(hi, lo, c)
	return q
}

// ptsToNano converts a track-timebase pts to nanoseconds since the session
// origin: pts * num * 1e9 / den.
func ptsToNano(pts uint64, info TrackInfo) uint64 {
	return mulDiv64(pts, uint64(info.TimebaseNum)*1_000_000_000, uint64(info.TimebaseDen))
}

// nanoToPTS converts nanoseconds since the session origin to a track pts:
// ns * den / (num * 1e9).
func nanoToPTS(ns uint64, info TrackInfo) uint64 {
	return mulDiv64(ns, uint64(info.TimebaseDen), uint64(info.TimebaseNum)*1_000_000_000)
}
```

- [ ] **Step 5: 구현 — reader accessor** (`gmc/reader.go`, `hasTrack` 아래에 추가)

```go
// trackInfo returns the TrackInfo for id from the live writer or the opened
// snapshot.
func (r *Reader) trackInfo(id TrackID) (TrackInfo, bool) {
	if r.w != nil {
		r.w.mu.Lock()
		defer r.w.mu.Unlock()
		ts, ok := r.w.tracks[id]
		if !ok {
			return TrackInfo{}, false
		}
		return ts.info, true
	}
	info, ok := r.tracks[id]
	return info, ok
}

// LastPTS returns the highest committed pts of the track. For live readers it
// reflects exactly the frames written so far; for opened files it comes from
// the footer summary or the recovery scan. ok is false when the track is
// unknown or has no frames.
func (r *Reader) LastPTS(id TrackID) (uint64, bool) {
	if r.w != nil {
		r.w.mu.Lock()
		defer r.w.mu.Unlock()
		ts, ok := r.w.tracks[id]
		if !ok || !ts.hasLast {
			return 0, false
		}
		return ts.maxPTS, true
	}
	v, ok := r.maxPTS[id]
	return v, ok
}

// LastTime returns the absolute wall-clock time of LastPTS:
// StartTime + LastPTS×timebase. ok is false without a start time, an unknown
// track, or an empty track.
func (r *Reader) LastTime(id TrackID) (time.Time, bool) {
	start, ok := r.StartTime()
	if !ok {
		return time.Time{}, false
	}
	pts, ok := r.LastPTS(id)
	if !ok {
		return time.Time{}, false
	}
	info, ok := r.trackInfo(id)
	if !ok {
		return time.Time{}, false
	}
	return start.Add(time.Duration(ptsToNano(pts, info))), true
}
```

- [ ] **Step 6: 통과 확인**

Run: `go test ./gmc/`
Expected: `ok`

- [ ] **Step 7: 커밋**

```powershell
git add gmc/timeconv.go gmc/timeconv_test.go gmc/reader.go gmc/lastpts_test.go
git commit -m "구현: 128비트 시간 변환 헬퍼 및 LastPTS/LastTime 라이브 엣지 조회 (3경로)"
```

---

### 태스크 5: SeekTime — 절대시간 시크

**Files:**
- Modify: `gmc/format.go` — `ErrNoStartTime` 추가, `gmc/reader.go` — `SeekTime` 추가
- Test: `gmc/seektime_test.go` (신규)

**Interfaces:**
- Consumes: 태스크 4 `nanoToPTS`·`trackInfo`, 기존 `idx.seek`·`Iterator`·`trackIDs`
- Produces(공개): `ErrNoStartTime`, `(r *Reader) SeekTime(t time.Time, tracks ...TrackID) (*Iterator, error)` — ReadInterleaved와 동일한 최소 오프셋 배치, 입력만 절대시간

- [ ] **Step 1: 실패하는 테스트 작성** — `gmc/seektime_test.go`

```go
package gmc

import (
	"errors"
	"testing"
	"time"
)

func TestSeekTime(t *testing.T) {
	w, _ := newTestWriter(t, CreateOptions{CheckpointBytes: 1 << 30, CheckpointInterval: time.Hour})
	start := time.Unix(1_700_000_000, 0).UTC()
	if err := w.SetStartTime(start); err != nil {
		t.Fatal(err)
	}
	// 90 kHz video: keyframe per 10 frames, pts step 3000 (30 frames = 1s)
	video, _ := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000})
	writeGOPs(t, w, video, 30)

	r, err := w.NewReader()
	if err != nil {
		t.Fatal(err)
	}
	defer r.Close()

	// t = start+0.5s -> pts 45000 -> starts at keyframe pts 30000
	it, err := r.SeekTime(start.Add(500 * time.Millisecond))
	if err != nil {
		t.Fatal(err)
	}
	if !it.Next() {
		t.Fatal(it.Err())
	}
	if got := it.Frame().PTS; got != 30000 {
		t.Fatalf("first pts = %d, want 30000", got)
	}

	// before start clamps to stream beginning
	it2, err := r.SeekTime(start.Add(-time.Hour), video)
	if err != nil {
		t.Fatal(err)
	}
	if !it2.Next() || it2.Frame().PTS != 0 {
		t.Fatalf("clamp: pts=%d err=%v", it2.Frame().PTS, it2.Err())
	}

	// unknown track
	if _, err := r.SeekTime(start, TrackID(99)); !errors.Is(err, ErrUnknownTrack) {
		t.Fatalf("unknown track: err = %v", err)
	}
	w.Close()
}

func TestSeekTimeRequiresStartTime(t *testing.T) {
	w, _ := newTestWriter(t, CreateOptions{})
	video, _ := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000})
	if err := w.WriteFrame(video, Frame{PTS: 0, Keyframe: true}); err != nil {
		t.Fatal(err)
	}
	r, err := w.NewReader()
	if err != nil {
		t.Fatal(err)
	}
	defer r.Close()
	if _, err := r.SeekTime(time.Now()); !errors.Is(err, ErrNoStartTime) {
		t.Fatalf("err = %v, want ErrNoStartTime", err)
	}
	w.Close()
}
```

- [ ] **Step 2: 실패 확인**

Run: `go test ./gmc/ -run TestSeekTime -v`
Expected: `FAIL ... [build failed]` (undefined: ErrNoStartTime / SeekTime)

- [ ] **Step 3: 구현**

`gmc/format.go`의 에러 블록에 추가:

```go
	ErrNoStartTime = errors.New("gmc: start time tag not set")
```

`gmc/reader.go`의 `ReadInterleaved` 아래에 추가:

```go
// SeekTime positions an interleaved iterator at the absolute wall-clock time
// t, converting it into each track's timebase (all tracks when none given).
// Placement follows ReadInterleaved: the minimum offset among each track's
// last sync point at or before the converted pts. Requires the
// gmc.start_time_unix_ns tag; returns ErrNoStartTime otherwise. Times before
// the start clamp to the beginning of the stream.
func (r *Reader) SeekTime(t time.Time, tracks ...TrackID) (*Iterator, error) {
	start, ok := r.StartTime()
	if !ok {
		return nil, ErrNoStartTime
	}
	var dns uint64
	if d := t.UnixNano() - start.UnixNano(); d > 0 {
		dns = uint64(d)
	}
	ids := tracks
	if len(ids) == 0 {
		ids = r.trackIDs()
	}
	filter := make(map[TrackID]bool, len(ids))
	startOff := r.committed.Load()
	for _, id := range ids {
		info, ok := r.trackInfo(id)
		if !ok {
			return nil, ErrUnknownTrack
		}
		filter[id] = true
		off, found := r.idx.seek(id, nanoToPTS(dns, info))
		if !found {
			off = r.streamStart
		}
		if off < startOff {
			startOff = off
		}
	}
	return &Iterator{r: r, off: startOff, filter: filter}, nil
}
```

- [ ] **Step 4: 통과 확인**

Run: `go test ./gmc/`
Expected: `ok`

- [ ] **Step 5: 커밋**

```powershell
git add gmc/format.go gmc/reader.go gmc/seektime_test.go
git commit -m "구현: SeekTime 절대시간 시크 (트랙별 timebase 변환, ErrNoStartTime)"
```

---

### 태스크 6: Finalized + 전체 회귀·vet

**Files:**
- Modify: `gmc/reader.go` — `finalized` 필드, `Open` 설정, `Finalized()` 메서드
- Test: `gmc/finalized_test.go` (신규)

**Interfaces:**
- Consumes: `loadFooter` 성공 경로
- Produces(공개): `(r *Reader) Finalized() bool` — 라이브 리더 항상 false

- [ ] **Step 1: 실패하는 테스트 작성** — `gmc/finalized_test.go`

```go
package gmc

import "testing"

func TestFinalized(t *testing.T) {
	w, path := newTestWriter(t, CreateOptions{})
	video, _ := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000})
	if err := w.WriteFrame(video, Frame{PTS: 0, Keyframe: true, Data: []byte("f")}); err != nil {
		t.Fatal(err)
	}

	// live reader: always false
	r, err := w.NewReader()
	if err != nil {
		t.Fatal(err)
	}
	if r.Finalized() {
		t.Fatal("live reader must not be finalized")
	}
	r.Close()

	if err := w.Finalize(); err != nil {
		t.Fatal(err)
	}
	r2, err := Open(path)
	if err != nil {
		t.Fatal(err)
	}
	if !r2.Finalized() {
		t.Fatal("finalized file must report true")
	}
	r2.Close()
}

func TestFinalizedFalseOnCrashedFile(t *testing.T) {
	w, path := newTestWriter(t, CreateOptions{})
	video, _ := w.AddTrack(TrackInfo{Kind: KindVideo, Codec: "h264", TimebaseNum: 1, TimebaseDen: 90000})
	if err := w.WriteFrame(video, Frame{PTS: 0, Keyframe: true, Data: []byte("f")}); err != nil {
		t.Fatal(err)
	}
	if err := w.Close(); err != nil { // no footer
		t.Fatal(err)
	}
	r, err := Open(path)
	if err != nil {
		t.Fatal(err)
	}
	defer r.Close()
	if r.Finalized() {
		t.Fatal("unfinalized file must report false")
	}
}
```

- [ ] **Step 2: 실패 확인**

Run: `go test ./gmc/ -run TestFinalized -v`
Expected: `FAIL ... [build failed]`

- [ ] **Step 3: 구현** (`gmc/reader.go`)

`Reader` 구조체에 필드 추가 (`maxPTS` 아래):

```go
	finalized bool
```

`Open`의 footer/scan 분기(68~71행)를 교체:

```go
	if err := r.loadFooter(size); err != nil {
		r.scan(size)
	} else {
		r.finalized = true
	}
```

`Close` 앞에 메서드 추가:

```go
// Finalized reports whether the file was properly closed with a footer and
// trailer. Live readers always report false (the file is still being written).
func (r *Reader) Finalized() bool {
	return r.finalized
}
```

- [ ] **Step 4: 통과 확인 + 전체 검증**

Run: `go test ./gmc/ -count=1` 그리고 `go vet ./gmc/`
Expected: `ok`, vet 출력 없음

- [ ] **Step 5: 커밋**

```powershell
git add gmc/reader.go gmc/finalized_test.go
git commit -m "구현: Finalized 완결 여부 판정 (footer 로드 성공 시에만 true)"
```

---

## 태스크 완료 후

- 전체 회귀: `go test ./gmc/ -count=1 -v` 전 태스크 PASS, `go vet ./gmc/` 클린.
- **`-race` 재실행 의무**: 이 개발 머신(windows/arm64)은 `-race` 미지원. 지원 플랫폼(linux/amd64 등) 또는 CI에서 `go test -race ./gmc/ -timeout 300s`를 반드시 실행하고 결과를 기록할 것.
- 후속: `docs/feature/2026-07-04-02-gmc-mkv-plan.md` (본 확장을 소비하는 gmc/codec + mkv 구현).

## 스펙 커버리지 매핑

| 설계 문서(2026-07-04-01) | 태스크 |
|---|---|
| §3.2 온디스크 flagHasDTS·레이아웃 | 1 |
| §3.3 Frame.DTS/HasDTS·TrackInfo.Reordered | 1, 2 |
| §3.4 검증 규칙 (기본 eff 단조 / Reordered 키프레임 pts) | 2 |
| §3.5 maxPTS 보정 (trackState·Finalize·스캔) | 2, 3 |
| §4 LastPTS/LastTime 3경로 | 3, 4 |
| §5 SeekTime·ErrNoStartTime·128비트 변환 | 4, 5 |
| §6 Finalized | 6 |
| §7 테스트 전략 | 각 태스크 + 완료 후 회귀 |
